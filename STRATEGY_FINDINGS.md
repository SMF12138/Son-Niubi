# 方向预测强化战果（研发历程记录）

> **性质：历史研发日志（append-only）**。记录方向信号从 ~50% 到 56–72% 的探索路径、试错与结论，供避免重复踩坑。
> 当前生产状态与最新回测数字以 **README.md** 为准；下一步计划见 **DEVELOPMENT.md**。

## 🏁 第五轮：系统审计 + 基建修复（2026-09-10）

全面审计 15+ 模块，修复 20+ 项问题（5 CRITICAL / 5 HIGH / 3 MEDIUM / 11 LOW）。

**关键修复**：forecast rate_df 丢失 → N=90 +0.1pp；新闻去重（23500→930行）；情绪词频升级+词典扩展；WAL模式；调度器重试→双层架构；死代码清理（macro_dir.py等）。

**准确率变化**：N=90 从 60.7%→60.8%（rate_df 修复），其余不变。基建修复不改模型逻辑。

## 🔥🔥🔥 第四轮：XGBoost + 每日信号探针 — 天花板确认（2026-09-10）

5 轮独立验证确认 56-68% 为免费数据+规则模型的真实上限：

**XGBoost 投票层探针（2次）**：
- 融合 z+油价+新闻+利率+均值回复+USD·RUB 特征
- 粗探针（gap=25）：N=7 XGB 53.3% vs 规则 56.0% → **落后 3pp**
- 精调探针（6组超参×gap=10×8000+次fit）：MOEX覆盖窗 XGB 48.7% vs 规则 67.6% → **XGB 把规则最强信号稀释了**
- **结论：XGB 此路不通，不接入产品**

**每日外部信号探针**：
- 单信号（USD/RUB/Brent/情绪/利率）：无 MOEX 期 2632 窗口，无一显著超基线 52%
- 组合加权：无 MOEX 窗口 +0.3-0.5pp，不值得接入
- **结论：免费可得的每日信号已到顶**

**参数微调（已完成落地）**：
- streak_min 3→2, confirm_bonus 0.03/0.015→0.02/0.01
- N=90 高置信 72.4%（+0.7%），全量不变

## 🏁 稳定阶段 — 最终战果定版(2026-09-09)

三轮燃烧 + 参数精调后定稿，进入稳定运行阶段。

**最终方向预测能力(7日, MOEX期, 无前视, win=150 + 4重确认):**

| 置信度档 | 实测命中 | 覆盖 | 含义 |
|---|---|---|---|
| conf≥0.85 | **83.7%** | 11% | |z|>1.5 & 3确认 — 王牌日 |
| conf=0.74 | 72.3% | 20% | 强信号 |
| 天天出手 | **67.5%** | 100% | 基准准确率 |

**从 50% 到 67.5%(天天)/83.7%(顶档) 的完整路径:**
1. MOEX 在岸价领先官方价的套利偏离(65.9%)
2. 4重确认闸门(MOEX动量/偏离加深/高波动/连续偏离)
3. 滚动窗 250→150 参数微调(+2.8点)
4. (|z|,确认数) 联合置信度分档 — 校准可信(标85%实戓83.7%)

**试过无增量(诚实记录)**：非线性GBM(比随机差)、三角分解、加权融合、成交量(board不提供)、拐点检测(样本少)。准确率天花板=天天出手 67.5%。

**稳定阶段监控**：automations 每日9点跑 monitor_signal，滚动命中率跌破 55% 告警到 QQ。

---

## 🔥🔥🔥 第三轮燃烧：参数微调 — 滚动窗 250→150, 全 horizon 持均提升

扫描滚动标准化窗口发现 **win=150 全面优于 250**(短窗更贴近当前市场状态):

| N | MOEX期(win250) | **MOEX期(win150)** | 顶档(conf≥0.76) |
|---|---|---|---|
| **7日** | 64.7% | **67.5%** | **79.0%**(n=186) |
| 30日 | 60.2% | **61.7%** | 72.9% |
| 60日 | 57.6% | **61.9%** | - |
| 90日 | 54.1% | **59.0%** | - |

**关键**：这是真·准确率提升(天天出手 65.9%→67.5%)，不是靠弃权。win=150 是甲点；更短(100)噪声升，更长(500)降到 58%。另测得死区 0.3(放弃|z|<0.3)可再抬 3-4 点但牺牲 32% 覆盖。

---

## 🔥🔥 第二轮燃烧：多重确认将 7 日顶置信推到 78.8%

在 MOEX 偏离主信号上叠加 3 个**当日可得、无前视**的确认信号：
1. MOEX 5日动量与 zdev 同向
2. 偏离加深(dev 近5日变化与 z 同号)
3. 高波动日(日内幅 ≥ 历史中位)

**实测质量-覆盖权衡(7日, MOEX期, 无前视):**

| 出手条件 | 准确率 | 覆盖 |
|---|---|---|
| 天天出手(基准) | 65.9% | 100% |
| 确认≥2 | 68.8% | 55% |
| 确认≥3 | 71.1% | 18% |
| **强偏离|z|>1 且 确认≥2** | **78.8%** | ~20% |

置信度已按「|z| + 确认数」分档写进 predictor。**最高置信档(conf≥0.76) 7日 78.8%(n=198)**。

讕譈的经验：任何“加权融合”都稀释纯zdev(降到63%)；正确做法是用确认信号做“闸门”(同向才出手/才高置信)，而非混入方向。三角分解(USD/RUB动量)、直接加权都无增量。

---

## 🔥 突破：MOEX 在岸价偏离信号 — 7日方向 64.7%（高置信 66%）

**数据源**：MOEX ISS API（莫斯科交易所在岸 CNYRUB_TOM 日线，免费无鉴权）— 这是卢布对人民币的**市场化成交价**，领先于 CBR 官方次日牌价。

**核心信号**：`dev = log(MOEX市场价) - log(CBR官方价)`。市场价高于官方 → 官方价未来上追 → CNY/RUB 上涨。这是**结构性套利偏离**，非过拟合。

**实测（滚动标准化 zdev，无前视 walk-forward, MOEX 覆盖期 2022-06 至今）：**

| N | 全样本(含2022前无MOEX稀释) | **MOEX期(实盘相关)** | MOEX高置信 |
|---|---|---|---|
| 1日 | - | 73.4% | 76.3% |
| 3日 | - | 71.2% | 73.9% |
| 5日 | - | 66.2% | 69.6% |
| **7日** | 55.2% | **64.7%** | **66.0%** |
| 30日 | 57.4% | 60.2% | 64.5% |
| 60日 | 56.9% | 57.6% | 63.2% |
| 90日 | 59.6% | 54.1% | 60.3% |

**突破点**：短窗（1-7日）在 MOEX 期全部 64%+，7日达 **64.7%**。短窗最强符合逻辑（官方价追市场价就在几天内完成）。早先“7日只有50%”的结论被推翻——不是信息天花板，是之前缺 MOEX 这个领先价。

**已落地**：
- `app/data/moex_rates.py`：fetch_moex_onshore/load_moex（存 SQLite moex_rates 表，1088 行）
- `app/models/moex_dir.py`：MoexDirectionPredictor（MOEX 偏离主导 + 均值回复 fallback），最新日无 MOEX 时回退最近 ≤3 交易日偏离
- forecast.py / cli.py 已切换；cmd_serve 启动时自动拓 MOEX；DIRECTION_JSON 含 moex_accuracy
- 前端方向表展示 全样本/MOEX期/MOEX高置信

---

## 已落地（前一轮）：MeanRevDirectionPredictor（现为 fallback）

2026-09-09 下午已将线性合成均值回复方向预测器正式接入生产，替代 mimo 的 XGBoost v4。

**新模型 `app/models/meanrev_dir.py::MeanRevDirectionPredictor`**（同接口，独立文件不动 v4）：
- 以 5 个均值回复特征（ma60_dev/ma20_dev/mom20/pos60/cusd_mom20）的滚动标准化合成分数为主导
- 强偏离（≥1σ）→ 直接均值回复方向，高置信；温和 → 常态概率（诚实低置信）
- 依赖 `mean_reversion.py::synthetic_meanrev_score`（滚动无前视）

**已切换调用点**：`forecast.py`(save_forecasts 方向字段)、`cli.py`(cmd_forecast/cmd_backtest) → MeanRevDirectionPredictor / meanrev_dir.run_direction_backtest。

**上线后仪表盘方向表（DIRECTION_JSON, 逐点 walk-forward）：**

| N | 全样本 | conf>60% | 随机基线 |
|---|---|---|---|
| 7 | 50.8% | 53.8% | ~52% |
| 30 | 53.8% | 57.8% | 51.9% |
| 60 | **56.4%** | **65.7%** | 51.8% |
| 90 | **61.1%** | **74.8%** | 54% |

**对比 mimo 原 XGBoost v4**（同数据回测）：v4 全 horizon 47-55% 贴基线，30/60 日低于随机；新模型长窗口明显超越。非线性模型把均值回复线性信号过拟合丢失，线性合成为正解。

## 核心发现回顾：极端均值回复是唯一稳定强信号

严格 walk-forward 滚动回测（无前视：MA20 只用 ≤t、波动率只用 ≤t 的 250 日窗）：

| 信号 | N=30 | N=60 | 备注 |
|---|---|---|---|
| 全样本 LR 方向 | 49.2% | 47.2% | 基本无信号 |
| MA20 极端偏离 ≥2σ 均值回复 | **77.7%** (139样本) | **77.7%** (139样本) | 覆盖率 ~4%（年约25天） |
| 与 LR 同向时 | 72.7% (strided 11) | **81.8%** (strided 11) | 更集中 |

- 规则：价格偏离 20 日均线 ≥2×滚动相对波动率 → 高偏离看跌、低偏离看涨（涨侧 N=60 up 至 90%+）。
- 早期用全样本 σ 的 88-93% 是事后偏差，滚动 σ 修正后 77.7% 才是诚实数字。
- 常识技术基线：MA20 均值回复全样本 51-57.7%；极端子集才有高精度。

## 新增文件（全部无前视、可复用）

> ⚠️ 2026-09-10：以下文件 2-5 已在系统审计中删除（死代码清理），仅保留 1。

1. `app/models/mean_reversion.py`（现存）：
   - `mean_rev_signal(close, i, N, k=2.0)` -> 单点信号
   - `mean_rev_series(close, N, k)` -> 批量触发点
2. ~~`app/models/strategy.py`~~（已删）
3. ~~`app/models/direction_ensemble.py`~~（已删）
4. ~~`app/models/baselines_dir.py`~~（已删）
5. ~~`app/calib.py`、`app/interval_pred.py`~~（已删）

## 推荐接入方向（给 mimo）

仪表盘方向卡升级为双模式：
1. 计算当前 dev20 vs 滚动 σ；若 ≥2σ → 显示"极端均值回归信号：未来30/60日看涨/看跌（历史精度77.7%）"
2. 否则 → 显示 LR 常态预测（注明"仅略优于随机"或给区间）。

## 诚实边界

- 77.7% 只在极端偏离+长窗口(30-60日)出现,覆盖率低;短窗口(N=7)仅 64.7%(17样本)不宣称。
- 均值回复全样本 51-57% 是"微弱但真实"的常识基线。
- 不构成投资建议。